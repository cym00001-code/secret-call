# References

这里放外部工具或设计工具生成的参考材料。它们帮助做视觉和交互判断，但不是生产工程的一部分。

当前目录：

- `kimi-app-prototype/`：Kimi 生成的前端原型。

使用规则：

- 不从这里直接发布。
- 不让这里参与根工程的 `lint`、`typecheck`、`build`。
- 不在这里实现安全协议、加密逻辑、房间状态机、服务端接口。
- 需要采用其中某个界面想法时，手工迁移到 `apps/web/`，并重新跑主项目测试。
